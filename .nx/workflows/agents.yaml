launch-templates:
  custom-linux-medium-plus-js:
    resourceClass: 'docker_linux_amd64/medium+'
    env:
      NX_CLOUD_ACCESS_TOKEN: '{{secrets.NX_CLOUD_ACCESS_TOKEN}}'
      NX_VERBOSE_LOGGING: 'true'
      NX_CLOUD_DEBUG_URLS: 'true'
    init-steps:
      - name: checkout repo
        uses: 'nrwl/nx-cloud-workflows/JamesHenry-patch-1/workflow-steps/checkout/main.yaml'
        env:
          # delete this comment
          GIT_CHECKOUT_DEPTH: 0
      - name: node_modules cache
        uses: 'nrwl/nx-cloud-workflows/main/workflow-steps/cache/main.yaml'
        env:
          KEY: 'yarn.lock'
          PATHS: 'node_modules'
          BASE_BRANCH: 'main'
      - name: NPM Install
        uses: 'nrwl/nx-cloud-workflows/main/workflow-steps/install-node-modules/main.yaml'
        env:
          SKIP_POSTINSTALL: 'true'
