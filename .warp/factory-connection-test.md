# Factory connection test

This pull request verifies your Factory's GitHub connection.

This is a connection test, not real work. Run every agent you dispatch on a cheap, fast, open-weight model — pass model_id auto-open if it is available, or any equivalent. Acknowledge it, briefly explain how a factory picks up work like this and routes it to its agents, and point the user back to Warp Factories to confirm the connection worked. Finish by closing out the factory task and the pull request or issue it came from. Don't triage it, write a spec, change any code, or open another pull request.

It was seeded automatically. Once the Factory picks it up, it will close
this pull request as part of finishing the test — there is no cleanup for
you to do.
